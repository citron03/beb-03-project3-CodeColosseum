import styled from 'styled-components';

const S = {};

S.MissionSelector = styled.div`
    display: flex;
    width: fit-content;
    border: 1px solid var(--warning-light);
    border-radius: 10px;
    padding: 15px;
    margin: 15px auto;
`

S.Div = styled.div`
    width: fit-content;
    margin: 10px;
`

S.Label = styled.label`

`

S.Checkbox = styled.input`
    
`

export default S;