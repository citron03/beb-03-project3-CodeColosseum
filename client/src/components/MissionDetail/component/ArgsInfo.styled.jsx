import styled from 'styled-components';

const S = {};

S.ArgInfo = styled.div`
    padding: 15px;
    max-width: 500px;
    border: 1px solid var(--dark-yellow);
    white-space: pre-line;
`

S.P = styled.p`
    font-size: 1.1rem;
`

export default S;