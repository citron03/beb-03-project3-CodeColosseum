import config from "../config"
import models from "../models";

// 콜로세움 종료시 토큰 리워드를 분배하는 함수입니다.
// 종결된 콜로세움 미션객체와 보상비율격체를 인자로 받습니다.
export = async function (): Promise<Object> {

    try {

        return {
        }
        
    } catch (error) {
        throw error;
    }
}